# Subsystems

A subsystem is a node of the graph: a local model with its own states, unknown
parameters and estimator.

::: pci.subsystem
    options:
      members: false
      show_root_heading: false

::: pci.subsystem.Subsystem

::: pci.subsystem.Unknown
