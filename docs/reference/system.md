# System

The interaction graph of subsystems and interfaces, plus the two solvers:
`estimate` (the distributed inverse problem) and `simulate` (the coupled forward
problem with the same message passing).

::: pci.system.System
